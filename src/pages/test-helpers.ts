// Re-export factories from workspace-factory to avoid duplication
// This file is kept for backwards compatibility with existing tests
export { createProjectPreview, createTaskPreview, createWorkspaceState } from '../test/workspace-factory'
